---
import {
  ArrowRight,
  BookOpenCheck,
  Compass,
  Database,
  LibraryBig,
  Microscope,
  Network,
  Radio,
  Telescope
} from "lucide-astro";
import BaseLayout from "@layouts/BaseLayout.astro";
import { getCollection } from "astro:content";
import { sections } from "@data/sections";
import { withBase } from "../../utils/paths";

const articles = await getCollection("articles");
const terms = await getCollection("glossary");
const resources = await getCollection("resources");

const scienceStats = [
  { value: articles.length, label: "articles", detail: "worlds, methods, atmospheres, missions, and cosmic context" },
  { value: terms.length, label: "terms", detail: "a compact vocabulary for reading the atlas" },
  { value: resources.length, label: "sources", detail: "missions, archives, catalogs, and scientific references" },
  { value: sections.length, label: "stations", detail: "world, signal, model, and careful conclusion" }
];

const atlasProtocol = [
  {
    title: "World",
    text: "Start with the physical environment: water, energy, chemistry, atmosphere, surface, orbit, and the host star.",
    icon: Telescope
  },
  {
    title: "Signal",
    text: "Look for measurable traces: spectra, transits, light curves, plume particles, minerals, or mission data.",
    icon: Database
  },
  {
    title: "Check",
    text: "Connect the signal to a model, then test whether instrument noise, stellar activity, or non-biological chemistry could explain it.",
    icon: Network
  }
];

const concepts = [
  {
    label: "condition",
    term: "Habitable zone",
    text: "The range of distances where liquid water could persist on a planet with suitable atmospheric pressure. It is not proof of habitability by itself."
  },
  {
    label: "signal",
    term: "Biosignature",
    text: "An observable feature that may be linked to life, such as a persistent atmospheric imbalance. It must be tested against non-biological explanations."
  },
  {
    label: "measurement",
    term: "Spectrum",
    text: "Light split by wavelength. Absorption and emission lines can reveal atoms, molecules, temperature, and the influence of a star."
  },
  {
    label: "method",
    term: "Transit method",
    text: "A planet is detected when it slightly dims its star while crossing the stellar disk. Repeated transits reveal size and orbital period."
  },
  {
    label: "environment",
    term: "Exoplanet atmosphere",
    text: "A gaseous envelope that shapes temperature, pressure, chemistry, and atmospheric loss. It is usually studied indirectly through spectra."
  },
  {
    label: "energy",
    term: "Tidal heating",
    text: "Internal heat generated by gravitational stretching. It can help maintain subsurface oceans on icy moons."
  }
];

const domains = [
  {
    title: "Habitable worlds",
    description: "Planets and moons are treated as physical systems: water, energy, chemistry, geology, and atmospheric stability.",
    href: "/en/sections/planetology/",
    tags: ["water", "energy", "chemistry"]
  },
  {
    title: "Methods and models",
    description: "Transits, spectra, radial velocities, and mission data turn distant objects into measurable evidence.",
    href: "/en/sections/astrophysics/",
    tags: ["spectra", "transits", "models"]
  },
  {
    title: "Cosmic environment",
    description: "A world cannot be read without its star, radiation field, galactic context, and observational limits.",
    href: "/en/sections/astronomy/",
    tags: ["stars", "radiation", "scale"]
  }
];

const materials = [
  {
    title: "Habitable worlds: what can be claimed carefully",
    description: "A framework for reading water, energy, chemistry, and uncertainty without jumping from conditions to life.",
    href: "/en/articles/habitable-worlds-framework/",
    section: "worlds",
    focus: "water and energy"
  },
  {
    title: "Biosignatures: why one gas is not proof",
    description: "A scientific signal becomes meaningful only after false positives and non-biological pathways are tested.",
    href: "/en/articles/biosignatures/",
    section: "atmospheres",
    focus: "cautious inference"
  },
  {
    title: "Spectra: reading atmospheres through light",
    description: "Spectroscopy links molecules, temperature, and stellar effects to the data we can actually measure.",
    href: "/en/articles/spectra/",
    section: "methods",
    focus: "wavelengths"
  },
  {
    title: "Icy moons and subsurface oceans",
    description: "Europa and Enceladus show why habitability can exist far from the classical habitable zone.",
    href: "/en/articles/icy-moons/",
    section: "worlds",
    focus: "subsurface oceans"
  },
  {
    title: "Stellar activity and habitability",
    description: "Flares, radiation, and atmospheric loss can decide whether a promising orbit remains physically plausible.",
    href: "/en/articles/stellar-activity-habitability/",
    section: "environment",
    focus: "host stars"
  },
  {
    title: "Telescopes and light",
    description: "The instruments behind the atlas: what telescopes measure, and why wavelength range matters.",
    href: "/en/articles/telescopes-and-light/",
    section: "missions",
    focus: "instruments"
  }
];

const evidenceScale = [
  {
    label: "observation",
    title: "What is measured",
    text: "Images, spectra, transits, radial velocities, plume particles, and surface geology provide the initial signal."
  },
  {
    label: "model",
    title: "How it is explained",
    text: "A physical model connects the signal to an ocean, atmosphere, orbit, chemistry, or planetary history."
  },
  {
    label: "limit",
    title: "Where caution starts",
    text: "The same signal can have several causes, so strong conclusions need repeated data and independent methods."
  }
];

const evidenceSteps = ["observation", "model", "limit"];

const sectionJumpLinks = [
  { href: "#world-atlas", label: "Atlas", detail: "worlds" },
  { href: "#science-method", label: "Method", detail: "signal" },
  { href: "#key-concepts", label: "Concepts", detail: "terms" },
  { href: "#sections", label: "Domains", detail: "routes" },
  { href: "#catalog", label: "Catalog", detail: "reading" }
];

const worlds = [
  {
    name: "Europa",
    category: "Icy moon",
    summary: "Jupiter's icy moon is a major target for studying subsurface oceans and possible chemically active environments.",
    signal: "surface geology and gravity-magnetic data are consistent with an ocean beneath the ice shell",
    evidence: "a strong ocean-world hypothesis now tested by Europa Clipper",
    limitation: "ocean composition, energy exchange, and surface-ocean connection still require direct constraints",
    facts: ["Jupiter", "ice shell", "subsurface ocean"],
    href: "/en/articles/icy-moons/",
    code: "JOV-EUR",
    type: "ocean world",
    distance: "628.3m km",
    coordinate: "Jupiter system",
    marker: "ice · ocean · tides",
    confidence: 2,
    tone: "cyan"
  },
  {
    name: "Enceladus",
    category: "Icy moon",
    summary: "Saturn's small moon ejects water vapor and ice grains, giving rare access to material from an internal ocean.",
    signal: "plumes carry water, salts, and organic compounds from the moon's interior",
    evidence: "Cassini flew through the plumes and measured particles directly",
    limitation: "water and organic chemistry are not the same as detected biology",
    facts: ["Saturn", "plumes", "salty ocean"],
    href: "/en/articles/icy-moons/",
    code: "SAT-ENC",
    type: "active plumes",
    distance: "1.27b km",
    coordinate: "Saturn system",
    marker: "plumes · salts · organics",
    confidence: 2,
    tone: "cyan"
  },
  {
    name: "Titan",
    category: "Icy moon",
    summary: "Titan has a dense nitrogen atmosphere, methane weather, and organic chemistry on a frozen surface.",
    signal: "surface liquids, atmospheric chemistry, and aerosols show an active carbon-rich environment",
    evidence: "Cassini-Huygens measured the atmosphere and mapped lakes, dunes, and haze",
    limitation: "its cold surface chemistry differs sharply from Earth-like water-based environments",
    facts: ["Saturn", "methane", "organic chemistry"],
    href: "/en/articles/planetary-surfaces/",
    code: "SAT-TIT",
    type: "exotic chemistry",
    distance: "1.43b km",
    coordinate: "Saturn system",
    marker: "nitrogen · methane · organics",
    confidence: 1,
    tone: "gold"
  },
  {
    name: "TRAPPIST-1e",
    category: "Rocky exoplanet",
    summary: "An Earth-size planet in a compact system around a cool red star, useful for testing atmospheres through transits.",
    signal: "repeated transits allow size estimates and atmospheric searches",
    evidence: "the system geometry is favorable for transmission spectroscopy",
    limitation: "a dense atmosphere and surface liquid water are not confirmed",
    facts: ["red dwarf", "transits", "habitable zone"],
    href: "/en/articles/exoplanets/",
    code: "TRP-1E",
    type: "rocky exoplanet",
    distance: "40.7 ly",
    coordinate: "TRAPPIST-1 star",
    marker: "transit · radius · atmosphere",
    confidence: 1,
    tone: "green"
  },
  {
    name: "Proxima Centauri b",
    category: "Nearest exoplanet",
    summary: "A planet around the closest star to the Sun, scientifically compelling but difficult because of stellar activity.",
    signal: "stellar radial-velocity motion indicates a planet with a near-Earth minimum mass",
    evidence: "detected by radial velocity, not by a direct surface image",
    limitation: "its atmosphere is unknown and may be affected by flares",
    facts: ["4.2 light-years", "radial velocity", "M dwarf"],
    href: "/en/articles/stellar-activity-habitability/",
    code: "PROX-B",
    type: "nearest exoworld",
    distance: "4.2 ly",
    coordinate: "red dwarf",
    marker: "radial velocity · flares",
    confidence: 1,
    tone: "gold"
  },
  {
    name: "Jezero",
    category: "Mars crater",
    summary: "An ancient Martian lake basin and delta, used to compare minerals, water history, and biosignature preservation.",
    signal: "delta sediments and hydrated minerals preserve a record of past water activity",
    evidence: "Perseverance studies rocks in place and stores samples for future analysis",
    limitation: "ancient habitability is not the same as evidence of past life",
    facts: ["Mars", "delta", "sample cache"],
    href: "/en/articles/planetary-surfaces/",
    code: "MRS-JEZ",
    type: "ancient delta",
    distance: "Mars",
    coordinate: "18.4° N · 77.6° E",
    marker: "delta · sediments · cores",
    confidence: 2,
    tone: "red"
  }
];
---

<BaseLayout
  title="CosmoSfera"
  lang="en"
  description="CosmoSfera: a scientific atlas of habitable worlds, observation methods, atmospheres, missions, and careful interpretation."
>
  <section class="container atlas-hero figma-hero" aria-labelledby="atlas-title">
    <div class="atlas-hero-copy">
      <p class="eyebrow hero-kicker">scientific atlas · habitable worlds</p>
      <h1 id="atlas-title" class="cosmosfera-logotype">
        <span>Cosmo</span><span>Sfera</span>
      </h1>
      <p class="hero-subtitle">Scientific atlas of habitable worlds</p>
      <p class="lead">
        CosmoSfera connects a world, a signal, and the limit of inference: from icy oceans and exoplanet atmospheres
        to missions, spectra, and models that show where the data ends.
      </p>
      <div class="hero-actions atlas-actions">
        <a class="button primary" href="#world-atlas">
          <Compass size={18} aria-hidden="true" /> Open the atlas
        </a>
        <a class="button" href={withBase("/en/knowledge/")}>
          <BookOpenCheck size={18} aria-hidden="true" /> Knowledge map
        </a>
        <a class="button" href={withBase("/")}>
          <ArrowRight size={18} aria-hidden="true" /> Russian version
        </a>
      </div>

      <div class="signal-strip" aria-label="Core research lines">
        <span><Radio size={16} aria-hidden="true" /> signal</span>
        <span><Microscope size={16} aria-hidden="true" /> data</span>
        <span><LibraryBig size={16} aria-hidden="true" /> limitation</span>
      </div>

      <nav class="hero-jump-nav" aria-label="Homepage quick navigation">
        {sectionJumpLinks.map((item, index) => (
          <a href={item.href}>
            <b>{String(index + 1).padStart(2, "0")}</b>
            <span>{item.label}</span>
            <small>{item.detail}</small>
          </a>
        ))}
      </nav>
    </div>

    <aside class="hero-command-panel" aria-label="Scientific atlas summary">
      <div class="command-panel-head">
        <span>scale</span>
        <strong>Observation → model → limit</strong>
      </div>
      <div class="hero-orbit-map" aria-hidden="true">
        <span class="orbit orbit-one"></span>
        <span class="orbit orbit-two"></span>
        <span class="orbit orbit-three"></span>
        <span class="world-dot dot-europa"></span>
        <span class="world-dot dot-titan"></span>
        <span class="world-dot dot-trappist"></span>
      </div>
      <div class="hero-scale-track" aria-label="Evidence scale">
        {evidenceSteps.map((step, index) => (
          <span>
            <b>{String(index + 1).padStart(2, "0")}</b>
            {step}
          </span>
        ))}
      </div>
    </aside>
  </section>

  <section id="world-atlas" class="container atlas-dashboard world-observatory" aria-labelledby="world-atlas-title">
    <div class="atlas-dashboard-shell">
      <div class="atlas-dashboard-head">
        <div>
          <p class="eyebrow">atlas of worlds and limitations · 6 entries</p>
          <h2 id="world-atlas-title">Worlds and limits of evidence</h2>
          <p>
            Passport cards connect each object, the observed signal, and the boundary of interpretation:
            where evidence is strong, where a model helps, and where caution must remain.
          </p>
        </div>
        <div class="dashboard-scale" aria-label="Evidence scale">
          {evidenceSteps.map((step) => (
            <span>{step}</span>
          ))}
        </div>
      </div>

      <div class="evidence-scale dashboard-evidence" aria-label="Scientific caution scale">
        {evidenceScale.map((item, index) => (
          <article>
            <span>{String(index + 1).padStart(2, "0")} · {item.label}</span>
            <h3>{item.title}</h3>
            <p>{item.text}</p>
          </article>
        ))}
      </div>

      <div class="atlas-passport-grid">
        {worlds.map((world, index) => (
          <article class={`world-passport world-passport--${world.tone}`}>
            <div class="passport-topline">
              <span>{world.code}</span>
              <span>{world.type}</span>
            </div>

            <div class="passport-heading">
              <span class="world-index">{String(index + 1).padStart(2, "0")}</span>
              <div>
                <p class="passport-label">{world.category}</p>
                <h3>{world.name}</h3>
              </div>
              <span class="passport-distance">{world.distance}</span>
            </div>

            <p class="passport-summary">{world.summary}</p>

            <div class="passport-meta-line">
              <span>{world.coordinate}</span>
              <span>{world.marker}</span>
            </div>

            <dl class="passport-readout">
              <div class="readout-cell readout-signal">
                <dt>Signal</dt>
                <dd>{world.signal}</dd>
              </div>
              <div class="readout-cell readout-data">
                <dt>Data</dt>
                <dd>{world.evidence}</dd>
              </div>
              <div class="readout-cell readout-limit">
                <dt>Limit</dt>
                <dd>{world.limitation}</dd>
              </div>
            </dl>

            <div class="confidence-line" aria-label={`Evidence scale: ${world.name}`}>
              {evidenceSteps.map((step, stepIndex) => (
                <span class={`confidence-dot ${stepIndex <= world.confidence ? "is-active" : ""}`}>{step}</span>
              ))}
            </div>

            <div class="topic-list world-facts">
              {world.facts.map((fact) => <span class="chip">{fact}</span>)}
            </div>

            <div class="topic-list world-links" aria-label={`Related material: ${world.name}`}>
              <a class="chip" href={withBase(world.href)}>Related material</a>
            </div>
          </article>
        ))}
      </div>
    </div>
  </section>

  <section id="science-method" class="container science-spine" aria-label="Research method">
    <div class="spine-intro">
      <p class="eyebrow">Research method</p>
      <h2>From world to testable signal</h2>
      <p>
        Habitability is not a claim about life. It starts with the physical environment and the data that can be measured:
        images, spectra, stars, surfaces, and missions.
      </p>
    </div>
    <div class="spine-steps">
      {atlasProtocol.map((step, index) => {
        const Icon = step.icon;
        return (
          <article class="spine-step">
            <span class="step-index">0{index + 1}</span>
            <Icon size={22} aria-hidden="true" />
            <h3>{step.title}</h3>
            <p>{step.text}</p>
          </article>
        );
      })}
    </div>
  </section>

  <section class="container portal-stats atlas-stats" aria-label="Site summary">
    {scienceStats.map((item) => (
      <div>
        <strong>{item.value}</strong>
        <span>{item.label}</span>
        <small>{item.detail}</small>
      </div>
    ))}
  </section>

  <section id="key-concepts" class="container concepts-atlas">
    <div class="section-heading">
      <div>
        <p class="eyebrow">Terms and limits</p>
        <h2>Key concepts</h2>
      </div>
      <p>Terms that help read the materials as a scientific atlas: from observation and measurement to model and cautious inference.</p>
    </div>
    <div class="concept-grid">
      {concepts.map((concept, index) => (
        <article class="concept-card">
          <span>
            <b>{String(index + 1).padStart(2, "0")}</b>
            {concept.label}
          </span>
          <h3>{concept.term}</h3>
          <p>{concept.text}</p>
        </article>
      ))}
    </div>
  </section>

  <section id="sections" class="container atlas-section">
    <div class="section-heading">
      <div>
        <p class="eyebrow">Research domains</p>
        <h2>Worlds, methods, and cosmic context</h2>
      </div>
      <p>The atlas connects planetary environments, observational methods, and the stellar context that shapes habitability.</p>
    </div>
    <div class="grid three section-atlas">
      {domains.map((domain) => (
        <article class="card section-card section-card--textual english-domain-card">
          <a class="card-link" href={withBase(domain.href)} aria-label={`Open ${domain.title}`}></a>
          <div class="card-body">
            <div class="article-meta article-card-meta">
              <span>atlas station</span>
            </div>
            <h3>{domain.title}</h3>
            <p>{domain.description}</p>
            <div class="topic-list">
              {domain.tags.map((tag) => <span class="chip">{tag}</span>)}
            </div>
          </div>
        </article>
      ))}
    </div>
  </section>

  <section id="catalog" class="container article-lab">
    <div class="section-heading">
      <div>
        <p class="eyebrow">Methods and explanations</p>
        <h2>Core reading route</h2>
      </div>
      <p>Selected materials act as English guideposts through observations, methods, limits, and source evidence.</p>
    </div>
    <div class="article-catalog">
      {materials.map((item) => (
        <article class="card article-card article-card--catalog english-catalog-card" data-section={item.section} data-tags={item.focus} data-title={item.title.toLowerCase()}>
          <a class="card-link" href={withBase(item.href)} aria-label={`Open ${item.title}`}></a>
          <div class="card-body">
            <div class="article-meta article-card-meta">
              <span>{item.section}</span>
              <span>guide</span>
            </div>
            <h3>{item.title}</h3>
            <p>{item.description}</p>
            <div class="article-protocol">
              <span>Focus</span>
              <strong>{item.focus}</strong>
            </div>
          </div>
        </article>
      ))}
    </div>
  </section>
</BaseLayout>
